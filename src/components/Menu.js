import React from "react";

const Menu = () => {

    return (
        <header>
            <a href="#">Home</a>
            <a href="#">Generate Worksheets</a>
        </header>
    );
}

export default Menu;